/**
 * @module lib/format
 *
 * @deprecated Re-export bridge — canonical source is `domain/format.ts`.
 * All new imports should use `from "../domain/format"` directly.
 */
export * from "../domain/format";
